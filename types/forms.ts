export interface DetailedFormData {
  // Add all the fields your form contains
  // For example:
  name?: string;
  email?: string;
  phone?: string;
  address?: string;
  description?: string;
  // Add other fields as needed based on your form structure
}
